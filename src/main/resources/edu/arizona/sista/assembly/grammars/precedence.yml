# assembly sub-tree moved to main taxonomy
# these rules make use of labels that exist in that taxonomy
# rather than duplicate it here, the assembly branch was grafted there
taxonomy: edu/arizona/sista/reach/biogrammar/taxonomy.yml

# vars for assembly sieve
vars:
  mylabel: "Assembly"
  deprulepriority: "1"
  surfacerulepriority: "2"
  # < operator should be outside of regex
  incomingbeforetimeprep: "</^prep_(before|prior_to)$/"
  incomingaftertimeprep: "</^prep_(after|following)$/"
  after_type: "PossibleController"
  # "after" should be some kind of Event that transforms "before"
  before_type: "PossibleController"
  beforesurface: "[lemma=/^(before|prior|precede)$/]"
  aftersurface: "[lemma=/^follow|after$/]"

# rules for assembly sieve
rules:
  # Event rules
  - name: assembly-prep-syntax-1
    label: ${mylabel}
    priority: ${deprulepriority}
    example: "A is ubiquitinated before the phosphorylation of B."
    pattern: |
      before:${before_type}
      # before the X
      after:${after_type} = ${incomingbeforetimeprep}

  - name: assembly-prep-syntax-2
    label: ${mylabel}
    priority: ${deprulepriority}
    example: "A is ubiquitinated following the phosphorylation of B."
    pattern: |
      after:${after_type}
      # after X
      before:${before_type} = ${incomingaftertimeprep}

  - name: assembly-prep-syntax-3
    label: DirectConnection
    priority: ${deprulepriority}
    example: "A is ubiquitinated due to the phosphorylation of B."
    pattern: |
      before:${before_type}
      after:${after_type} = <prep_due_to | <prep_because_of

  - name: assembly-verb-syntax-1
    label: DirectConnection
    priority: ${deprulepriority}
    example: "the ubiquitination of A leads to the phosphorylation of B."
    pattern: |
      before:${before_type}
      after:${after_type} = <nsubj [lemma=/^(lead)$/] prep_to

  - name: assembly-verb-syntax-2
    label: DirectConnection
    priority: ${deprulepriority}
    example: "the ubiquitination of A precedes the phosphorylation of B."
    pattern: |
      before:${before_type}
      after:${after_type} = <nsubj [lemma=/^(precede)$/] dobj

  - name: assembly-verb-syntax-3
    label: DirectConnection
    priority: ${deprulepriority}
    example: "the ubiquitination of A causes the phosphorylation of B."
    pattern: |
      after:${after_type}
      before:${before_type} = <nsubj [lemma=/^(precipitate|cause|follow)$/] dobj

  - name: assembly-verb-syntax-4
    example: "A ubiquitination is a result of B phosphorylation."
    label: ${mylabel}
    priority: ${deprulepriority}
    pattern: |
     before:${after_type}
     after:${before_type} = </prepc?_of/ [lemma=result] nsubj

  - name: assembly-verb-syntax-5
    example: "A is ubiquitinated, resulting in the phosphorylation of B."
    label: ${mylabel}
    priority: ${deprulepriority}
    pattern: |
     after:${after_type}
     before:${before_type} = <prep_in [lemma=result & tag=VBG] >vmod

  - name: assembly-verb-syntax-6
    example: "As a result of A phosphorylation, B is ubiquitinated. A is phosphorylated as a result of B ubiqutination."
    label: ${mylabel}
    priority: ${deprulepriority}
    pattern: |
     after:${after_type}
     before:${before_type} = prep_as [lemma=result] /prepc?_of/

  - name: assembly-verb-syntax-7
    example: "A phosphorylation results in B ubiqutination."
    label: ${mylabel}
    priority: ${deprulepriority}
    pattern: |
     before:${before_type}
     after:${after_type} = <nsubj [lemma=result] prep_in

  # Passives
  - name: assembly-syntax-pass-1
    label: DirectConnection
    priority: ${deprulepriority}
    example: "the ubiquitination of A is followed by the phosphorylation of B."
    pattern: |
      before:${before_type}
      after:${after_type} = <nsubjpass [lemma=/^(precipitate|cause|follow)$/] agent

  - name: assembly-syntax-pass-2
    label: DirectConnection
    priority: ${deprulepriority}
    example: "the ubiquitination of A is preceded by the phosphorylation of B."
    pattern: |
      after:${after_type}
      before:${before_type} = <nsubjpass [lemma=/^(precede)$/] agent

  ############################
  # Surface rules
  ############################

  # finds a direct connection
  - name: assembly-surface-1
    example: "A is ubiquitination immediately follows the phosphorylation of B. A is ubiquitinated immediately following the phosphorylation of B.  A ubiquitination is immediately followed by the phosphorylation of B."
    label: DirectConnection
    type: token
    priority: ${surfacerulepriority}
    pattern: |
      # "A precedes B" pattern
      @before:${after_type} [lemma=be]? [lemma=/^immediate|direct$/] ${beforesurface} [tag=IN]? [tag=DT]? @after:${before_type}
      |
      # "A follows B" pattern
      @before:${after_type} [lemma=be]? [lemma=/^immediate|direct$/] ${aftersurface} [tag=IN]? [tag=DT]? @after:${before_type}

  # finds a direct connection
  - name: assembly-surface-2
    example: "A is ubiquitinated following the phosphorylation of B.  A is ubiquitinated prior to the phosphorylation of B."
    label: ${mylabel}
    type: token
    priority: ${surfacerulepriority}
    pattern: |
      # "A precedes B" pattern
      @before:${after_type} [lemma=be]? ${aftersurface} [tag=IN]? [tag=DT]? @after:${before_type}
      |
      # "A follows B" pattern
      @before:${after_type} [lemma=be]? ${beforesurface} [tag=IN]? [tag=DT]? @after:${before_type}

  - name: assembly-surface-3
    example: "A is ubiquitinated in response to the phosphorylation of B."
    label: ${mylabel}
    type: token
    priority: ${surfacerulepriority}
    pattern: |
     @after:${before_type} in response to []{,2} @before:${after_type}

  - name: assembly-surface-4
    example: "X ubiquitination has downstream effects on the phosphorylation of Y."
    label: ${mylabel}
    type: token
    priority: ${surfacerulepriority}
    pattern: |
     # NOTE: event "A has no effect on B" implies A before B
     @before:${after_type} [lemma=have] []{,2} [lemma=effect] on []{,2} @after:${before_type}

  - name: assembly-surface-5
    example: "A ubiquitination is a result of B phosphorylation."
    label: ${mylabel}
    type: token
    priority: ${surfacerulepriority}
    pattern: |
     @after:${before_type} [lemma=be] result of []{,2} @before:${after_type}

  - name: assembly-surface-6
    example: "A is ubiquitinated, resulting in the phosphorylation of B."
    label: ${mylabel}
    type: token
    priority: ${surfacerulepriority}
    pattern: |
     @before:${after_type} []{,2} [lemma=result] in []{,2} @after:${before_type}

  - name: assembly-surface-7
    example: "A is ubiquitinated, thereby increasing the phosphorylation of B."
    label: ${mylabel}
    type: token
    priority: ${surfacerulepriority}
    pattern: |
     @before:${after_type} []{,2} [lemma=thereby] []{,3} @after:${before_type}