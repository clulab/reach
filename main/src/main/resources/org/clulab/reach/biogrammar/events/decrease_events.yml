# Decrease_amount events require a theme of BioChemicalEntity

- name: decrease_amount_1
  priority: ${ priority }
  example: "loss of neurofibromin"
  label: DecreaseAmount
  pattern: |
    trigger = [lemma=/${ triggerLemma }/]
    theme:BioChemicalEntity = nmod_of | nmod_in
