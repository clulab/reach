
#
# Template for negative regulation|activation
#

vars: org/clulab/reach/biogrammar/vars.yml

rules:

- name: Negative_${ ruleType }_syntax_1_verb
  priority: ${ priority }
  example: "X inhibits Y"
  label: ${ label }
  action: ${ actionFlow }
  pattern: |
    trigger = [word=/(?i)^(${ triggers })/ & tag=/^V|RB/] [lemma=/^(${ auxtriggers })/ & tag=/^V/]?
    controlled:${ controlledType } = (${adverbial_clause}|${adjective_clause})? (${objects} | ${complements}) /${conjunctions}|${dep}|${objects}|${noun_modifiers}|nmod_of|nmod_in$/{,2} (>> [word=by]){,2}
    controller:${ controllerType } = </${complements}/? (${agents} | ${nominal_passive_agents} | nsubjpass | </${adverbial_clause}|${adjective_clause}/) /${noun_modifiers}|${conjunctions}|nmod_of|nmod_in$/{,2}
# Note: Captures passive voice; thus verb must be VBD|VBN
- name: Negative_${ ruleType }_syntax_2_verb
  priority: ${ priority }
  example: "X is inhibited by Y"
  label: ${ label }
  action: ${ actionFlow }
  pattern: |
    trigger = [word=/(?i)^(${ triggers })/ & tag=/VBD|VBN|RB/] [lemma=/^(${ auxtriggers })/ & tag=/^V/]?
    controlled:${ controlledType } = (>nsubjpass|</${adverbial_clause}|${adjective_clause}/) /${conjunctions}|${noun_modifiers}/{,2}
    controller:${ controllerType } = (${passive_agents}) /${conjunctions}|${noun_modifiers}|nmod_of|nmod_in$/{,2}
- name: Negative_${ ruleType }_syntax_3_verb
  priority: ${ priority }
  example: ""
  label: ${ label }
  action: ${ actionFlow }
  pattern: |
    trigger = [word=/(?i)^(${ triggers })/ & !outgoing=nsubjpass & tag=/^V|RB/] [lemma=/^(${ auxtriggers })/ & tag=/^V/]?
    controlled:${ controlledType } = ( (/nmod_[^b]|${conjunctions}|${noun_modifiers}/{1,2}) | (< /nmod_|${conjunctions}|${noun_modifiers}/{1,2}) )
    controller:${ controllerType } = </${noun_modifiers}/{,2} | (/${passive_agents}/) /${noun_modifiers}|${conjunctions}|nmod_of|nmod_in$/{,2}
- name: Negative_${ ruleType }_syntax_4_verb
  priority: ${ priority }
  example: ""
  label: ${ label }
  action: ${ actionFlow }
  pattern: |
    trigger = [word=/(?i)^(${ triggers })/ & tag=/^V|RB/] [lemma=/^(${ auxtriggers })/ & tag=/^V/]?
    controlled:${ controlledType } = (nsubjpass /${conjunctions}|${noun_modifiers}/{,2})
    controller:${ controllerType } = (/nmod_/ /${conjunctions}|${noun_modifiers}/{,2})? /nmod_/{,2} /${passive_agents}/ /${conjunctions}|${noun_modifiers}/{,2}
- name: Negative_${ ruleType }_syntax_5_verb
  priority: ${ priority }
  example: ""
  label: ${ label }
  action: ${ actionFlow }
  pattern: |
    trigger = [word=/(?i)^(${ triggers })/ & tag=/^V|RB/] [lemma=/^(${ auxtriggers })/ & tag=/^V/]?
    controlled:${ controlledType } = ${objects} /${noun_modifiers}|${conjunctions}/{,2}
    controller:${ controllerType } = (< /${complements}|${standalone_appositive}/ | </${relative_clause}/){1,2} /${noun_modifiers}|${conjunctions}/{,2}
- name: Negative_${ ruleType }_syntax_6_verb
  priority: ${ priority }
  example: ""
  label: ${ label }
  action: ${ actionFlow }
  pattern: |
    trigger = [word=/(?i)^(${ triggers })/ & tag=/VBN|RB/] [lemma=/^(${ auxtriggers })/ & tag=/^VBN/]?
    controlled:${ controlledType } = (</${complements}|${standalone_appositive}/ | </${relative_clause}/){1,2} /${noun_modifiers}|${conjunctions}/{,2}
    controller:${ controllerType } = /${passive_agents}/ /${noun_modifiers}|${conjunctions}|nmod_of|nmod_in$/{,2}
- name: Negative_${ ruleType }_syntax_7_verb
  priority: ${ priority }
  example: "Cells were additionally inhibited with 10 ng/ml leptin and cell extracts analyzed for ErbB3 tyrosine phosphorylation."
  label: ${ label }
  action: ${ actionFlow }
  pattern: |
    trigger = [word=/(?i)^(${ triggers })/ & tag=VBN] # passive verb
    controlled:${ controlledType } = nmod_with (${adverbial_clause}|${adjective_clause}) nmod_for
    controller:${ controllerType } = nmod_with

- name: Negative_${ ruleType }_syntax_1_noun
  priority: ${ priority }
  example: ""
  label: ${ label }
  action: ${ actionFlow }
  pattern: |
    trigger = [word=/(?i)^(${ triggers })/ & tag=/^N/] [lemma=/^(${ auxtriggers })/]?
    controlled:${ controlledType } = /nmod_of|nmod_in$/ /${noun_modifiers}|${conjunctions}/{,2}
    controller:${ controllerType } = /nmod_of/? /${passive_agents}/ (</${objects}/|${passive_agents}|${noun_modifiers}|nmod_of|nmod_in$){1,2} /${noun_modifiers}|${conjunctions}/{,2} # the nmod_of may appear due to bad syntax

- name: Negative_${ ruleType }_syntax_2_noun
  priority: ${ priority }
  example: ""
  label: ${ label }
  action: ${ actionFlow }
  pattern: |
    trigger = [word=/(?i)^(${ triggers })/ & tag=/^N/] [lemma=/^(${ auxtriggers })/ & tag=/^N/]?
    controlled:${ controlledType } = /nmod_of|nmod_in$/? /${conjunctions}|${noun_modifiers}/{1,2}
    controller:${ controllerType } = /nmod_of/? /${passive_agents}/ /${conjunctions}|${noun_modifiers}/{,2} # the nmod_of may appear due to bad syntax

- name: Negative_${ ruleType }_syntax_3_noun
  priority: ${ priority }
  example: "rapamycin inhibition of the phosphorylation of ERK"
  label: ${ label }
  action: ${ actionFlow }
  pattern: |
    trigger = [word=/(?i)^(${ triggers })/ & tag=/^N/ & !outgoing=/${passive_agents}/] [lemma=/^(${ auxtriggers })/ & tag=/^N/]?
    controlled:${ controlledType } = /nmod_of/ /${conjunctions}|${noun_modifiers}/{,2}
    controller:${ controllerType } = /${noun_modifiers}/{1,2} | ${conjunctions} /${passive_agents}/ compound? 

- name: Negative_${ ruleType }_syntax_5_noun
  priority: ${ priority }
  example: ""
  label: ${ label }
  action: ${ actionFlow }
  pattern: |
    trigger = [word=/(?i)^(${ triggers })/ & tag=/^N/] [lemma=/^(${ auxtriggers })/ & tag=/^N/]?
    controlled:${ controlledType } = < /${noun_modifiers}/ > /nmod_/ /${conjunctions}|${noun_modifiers}/{,2}
    controller:${ controllerType } = < /${noun_modifiers}/ /${conjunctions}|${noun_modifiers}/{,2}

- name: Negative_${ ruleType }_syntax_6_noun
  priority: ${ priority }
  example: ""
  label: ${ label }
  action: ${ actionFlow }
  pattern: |
    trigger = [word=/(?i)^(${ triggers })/ & tag=/^N/] [lemma=/^(${ auxtriggers })/ & tag=/^N/]?
    controller:${ controllerType } = <nmod_via (${passive_agents}) /${noun_modifiers}|${conjunctions}/{,2}
    controlled:${ controlledType } = (/nmod_of|nmod_in$/ /${noun_modifiers}|${conjunctions}/{,2}){1,2}

- name: Negative_${ ruleType }_syntax_7_noun
  priority: ${ priority }
  example: "Rapamycin, an inhibitor of the mTOR kinase,"
  label: ${ label }
  action: ${ actionFlow }
  pattern: |
    trigger = [word=/(?i)^(${ triggers })/ & tag=/^N/] [lemma=/^(${ auxtriggers })/ & tag=/^N/]?
    controller:${ controllerType } = </${dep}/ /${noun_modifiers}|${conjunctions}/{,2}
    controlled:${ controlledType } = (/nmod_of|nmod_in$/ /${noun_modifiers}|${conjunctions}/{,2}){1,2}
# Captures common Hearst patterns: hypernym such as hyponym
- name: Negative_${ ruleType }_syntax_noun_Hearst
  priority: ${ priority }
  example: "HDAC inhibitors including trichostatin A"
  label: ${ label }
  action: ${ actionFlow }
  pattern: |
    trigger = [word=/(?i)^(${ triggers })/ & tag=/^N/] [lemma=/^(${ auxtriggers })/ & tag=/^N/]?
    controlled:${ controlledType } = /${conjunctions}|${noun_modifiers}/{,2}
    controller:${ controllerType } = /nmod_such_as|nmod_including|nmod_like/ (compound|${conjunctions})?
- name: Negative_${ ruleType }_token_1_noun
  priority: ${ priority }
  example: "mTOR inhibitor Rapamycin"
  type: token
  label: ${ label }
  action: ${ actionFlow }
  pattern: |
    @controlled:${ controlledType } (?<trigger> [word=/(?i)^(${ triggers })/ & tag=/^NN/]) @controller:${ controllerType }
# this rule is needed because PP attachment of "by" is often wrong
- name: Negative_${ ruleType }_token_2_noun
  priority: ${ priority }
  example: "Inhibition of mTOR by rapamycin has been standard treatment "
  type: token
  label: ${ label }
  action: ${ actionFlow }
  pattern: |
    (?<trigger> [word=/(?i)^(${ triggers })/ & tag=/^NN/]) of @controlled:${ controlledType } by @controller:${ controllerType } (?! [lemma=/${ triggers }/ & tag=/^NN/])
- name: Negative_${ ruleType }_copula_1
  priority: ${ priority }
  example: "A is an inhibitor of B"
  label: ${ label }
  action: ${ actionFlow }
  pattern: |
    trigger = [lemma=/be/ & tag=/^V/] []? []? [lemma=/${ triggers }/ & tag=/^N/]
    controller:${ controllerType } = ${agents}
    controlled:${ controlledType } = /nmod_of|nmod_in$/
- name: Negative_${ ruleType }_possessive_noun
  priority: ${ priority }
  example: "ASPP1 is common, as is its inhibition by ASPP2."
  label: ${ label }
  action: ${ actionFlow }
  pattern: |
    trigger = [lemma=/${ triggers }/ & tag=/^N/]
    controller:${ controllerType } = /${passive_agents}/
    controlled:${ controlledType } = /"nmod:poss"/
# a rule written to catch an example from MITRE
# currently unclear how common this pattern may be...
- name: Negative_${ ruleType }_token_experimental_results_1
  priority: ${ priority }
  type: token
  example: "Cells were additionally inhibited with 10 ng/ml leptin and cell extracts analyzed for ErbB3 tyrosine phosphorylation."
  label: ${ label }
  action: ${ actionFlow }
  pattern: |
    # passive verb ("inhibited" in the example)
    (?<trigger> [word=/(?i)^(${ triggers })/ & tag=VBN])
    # "leptin" in our example
    with /./{,2} @controller:${ controllerType }
    # analyzed, examined, etc.
    # hopefully this isn't too noisy...
    and /./{,3} [tag=/^V/] for
    # the phosphorylation in our example
    @controlled:${ controlledType }
